pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'stevie was here and doing something on jenkins'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
